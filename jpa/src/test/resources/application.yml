spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backlog
    username: back
    password: abcd1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true
